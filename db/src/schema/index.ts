// Export all schema definitions from this file
// Add your table schemas here or import them from separate files

// Example schema structure (uncomment and modify as needed):
// import { pgTable, serial, text, timestamp } from "drizzle-orm/pg-core";
//
// export const users = pgTable("users", {
//   id: serial("id").primaryKey(),
//   name: text("name").notNull(),
//   email: text("email").notNull().unique(),
//   createdAt: timestamp("created_at").defaultNow().notNull(),
// });

// For now, export an empty object to allow the package to work
export {};

